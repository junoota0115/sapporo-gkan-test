<% return if params[:action] =~ /index/ %>

<nav class="nav-menu">
  <% if params[:action] =~ /show/ %>
    <%= link_to t('ss.links.back_to_index'), action: :index %>
    <%= link_to t('ss.links.delete'), action: :delete %>
  <% else %>
    <%= link_to t('ss.links.back'), action: :show %>
  <% end %>
</nav>
